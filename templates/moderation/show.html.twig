{% extends 'base.html.twig' %}

{% block title %}Moderation{% endblock %}

{% block body %}
    <h1>Moderation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moderation.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ moderation.title }}</td>
            </tr>
            <tr>
                <th>Introduction</th>
                <td>{{ moderation.introduction }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ moderation.content }}</td>
            </tr>
            <tr>
                <th>Youtube</th>
                <td>{{ moderation.youtube }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('moderation_index') }}">back to list</a>

    <a href="{{ path('moderation_edit', {'id': moderation.id}) }}">edit</a>

    {{ include('moderation/_delete_form.html.twig') }}
{% endblock %}
